// Shared free-text refine suggestion chips, used by both the single-draft
// RefinementPanel and the deck-slide refine control (T9) so the two surfaces
// offer the identical chip set (no deck-specific suggestions in v1).
export const REFINE_SUGGESTIONS = [
  'Make the background darker',
  'Move the headline to top',
  'Increase font size',
]
