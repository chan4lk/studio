# bistec-studio — Claude Context

This repo contains planning documents for **bistec-studio**, an internal marketing post generation tool for the Bistec marketing team.

## ✅ Outstanding work — START HERE (updated 2026-08-07)

**⏸️ PICK UP HERE — prod is unblocked and self-migrating. The open items are B4 (scheduler resource not running) + no team Claude token on either prod team, then the remaining prod tests** (docs: [`docs/handoff.md`](docs/handoff.md) ← 2026-07-28 top section, [`docs/prod-e2e-findings-2026-07-24.md`](docs/prod-e2e-findings-2026-07-24.md), [`docs/prod-e2e-findings-2026-07-23.md`](docs/prod-e2e-findings-2026-07-23.md), [`docs/security-prompt-injection-review-2026-07-22.md`](docs/security-prompt-injection-review-2026-07-22.md)):

- **✅ 2026-08-07 — Deck library consolidation + per-slide prompted refine** (specclaw change `deck-library-consolidation`; spec/design/tasks in `.specclaw/changes/deck-library-consolidation/`). Two independent gaps closed on top of BL-07 (2026-08-04, below): decks were invisible outside the dedicated `/decks/[id]` review page, and a slide's design could only be regenerated parameterlessly (no instruction).
  - **Decks now surface in the main Library.** `GET /api/library`'s response envelope changed from `{drafts}` to `{items}` — a discriminated union `{type:'post',...DraftRecord} | {type:'deck',...DeckLibraryItem}`. Backend: a pure `mergeLibraryItems` helper (`src/lib/library/mergeLibraryItems.ts`) merges two independently-paginated, already-sorted sources (Drafts with `deckSlide: null` — a deck's own slide Drafts never double-count as standalone posts — and Decks under `deckVisibilityWhere`) into one correctly-ordered/sliced page. Decks **ignore the `status` filter** (no per-post status — `READY`/`PUBLISHED`/`SCHEDULED`/`FAILED` — has a clean 1:1 deck equivalent) but do respect `search`. Each deck's `slideCount`/`readySlideCount`/`thumbnailUrl` are derived from its slides' Draft `status`/`exportUrl` at query time — **never from `Deck.status`**, which in the current implementation never reaches `GENERATING`/`READY`. Frontend: new `DeckCard` (visual sibling to `PostCard`, `src/components/library/DeckCard.tsx`) with an "Export as PPTX" button gated on `readySlideCount === slideCount`; the Library grid renders `PostCard`/`DeckCard` per `item.type` via an exhaustive switch.
  - **Per-slide prompted refine.** The deck review page's slide cards gained a free-text instruction input + the same suggestion chips as the single-draft `RefinementPanel` (`REFINE_SUGGESTIONS` extracted to `src/lib/drafts/refineSuggestions.ts` and shared). **No deck-scoped refine endpoint was added** — the deck page POSTs straight to the existing `POST /api/drafts/[id]/refine` with that slide's `draftId`, then relies on the same poll loop `handleRegenerateDesign` already uses (`exportUrl` diff against a captured baseline). `GET /api/decks/[id]` now also surfaces `hasPendingConflict` per slide (from `Draft.pendingConflict !== null`, never the raw withheld HTML) so a refine that lands in a brand-kit conflict resolves the poll immediately (a clean completion, not a pending regenerate) instead of waiting out the timeout — the slide shows an inline "needs your review" link to `/drafts/{draftId}` instead of spinning.
  - **No schema change, no migration.**
  - Gates: tsc clean, lint 0 errors (unchanged pre-existing warning count), **407/407 unit** (new `tests/unit/library/mergeLibraryItems.test.ts`). E2E: `library.test.ts` (§H) grew 5→**10 cases** (TC-LIB-06..10, all green), `deck-generation.test.ts` (§U) grew 4→**6 cases** (TC-DECK-05/06, all green); full catalog run **167 passed / 4 skipped / 9 failed** — every failure is pre-existing and unrelated to this diff: the same 5 browser-login `next dev`/Turbopack HMR fetch-abort flakes already documented in the 2026-08-04 entry below (`ui.test.ts` ×5, `settings-claude-token.test.ts` UI case ×2, `campaign-scheduling.test.ts` UI case) plus one newly-surfaced **pre-existing** data-volume issue: `team-isolation.test.ts`'s `findCampaignByName` reads the unfiltered `GET /api/campaigns` (capped `take: 200`, no search param), and months of accumulated test-run data on this shared dev/test DB have pushed the seeded "Bistec Campaign" fixture off page 1 — the exact class of problem this same test file already worked around for drafts via `findDraftBySearch`'s `?search=`, just not yet applied to campaigns. Not touched by this branch's diff; reproduces independent of it. **🧠 Sandbox note:** the long-lived local `next dev -p 3001` test server (running since 2026-08-04) had cached a system-user id (`getSystemUserId`'s in-process `cachedId`) from before a later `test:e2e:db` reset the test DB, so ACP-surface generation 422'd on a `TeamMembership` FK violation — restarting the test server (not the DB) resolved it. Any long-lived `test:e2e:serve` process should be recycled after re-running `test:e2e:db`.
- **✅ 2026-08-04 — "Slide Deck" generation flow (BL-07)** (specclaw change `slide-deck-generation`; spec/design/tasks in `.specclaw/changes/slide-deck-generation/`). New user-facing flow, distinct from BL-06 (which exports an _existing_ single post) — this generates a brand-new multi-slide deck from its own brief. Architecture: `Deck` is to `Brief` what `DeckSlide` is to `Draft` — each approved outline entry becomes an ordinary `Brief` + `Draft` row (deck's brand kit/campaign/aspect ratio/design mode/tone/goal carried down) plus a linking `DeckSlide(deckId, draftId, orderIndex, topic)` row, so Path A/B generation, Puppeteer rendering, and per-slide regenerate are **all reused completely unmodified** — the deck layer only adds bookkeeping.
  - **Flow:** brief inputs (topic/prompt/brand kit/campaign/tone/goal/images, no manual slide count) → `POST /api/decks` → `POST /api/decks/[id]/outline` (Haiku proposes `{topic, hint}[]`, capped at `MAX_DECK_SLIDES=15`, one-generic-slide fallback if the model returns zero) → `OutlineReviewStep` (add/remove/edit/reorder before approving) → `POST /api/decks/[id]/outline/approve` creates all N Brief+Draft+DeckSlide rows in one **all-or-nothing per-slide transaction** (one slide's row-creation failure rolls back every slide from that approve call), then fires `startBackgroundGeneration` per slide — **independent fan-out, no cross-slide transaction**, so one slide's generation crashing can never affect another's. Deck review page polls `GET /api/decks/[id]` (deck + slides, each surfacing its Draft's status/exportUrl/failureReason), offers per-slide regenerate (`POST /api/decks/[id]/slides/[slideId]/regenerate-design`, same 202/409 claim semantics as the single-draft route via `claimDeckSlideAction`) and delete-slide (re-indexes remaining `orderIndex` in a transaction), and an "Export as PPTX" button disabled until every slide is `EXPORTED`.
  - **Multi-slide pptx export** (`POST /api/decks/[id]/export/pptx`) extends the existing single-slide `src/lib/export/pptx.ts` (factored out a shared `addImageSlide` helper — the existing single-slide `buildPptxBuffer` contract is unchanged) with `buildMultiSlidePptxBuffer`: fetches each slide's PNG from the EXPORTS bucket in `orderIndex` order, one full-bleed image slide per PNG, sized to the deck's own aspect ratio; 422 if any slide's Draft isn't `EXPORTED` yet.
  - **New "Deck" schema** (`Deck`, `DeckSlide`, `DeckStatus` enum) — `DeckSlide.draftId` is a unique FK to `Draft` (one Draft per slide); `@@unique([deckId, orderIndex])`. No new column on `Brief` or `Draft`. Deck visibility mirrors the existing D6 semantics (`deckVisibilityWhere` in `src/lib/authz/visibility.ts` — own + campaign-shared for editors, all-team for admin/super-admin, cross-team always 404 never 403).
  - **Entry point:** the sidebar "New post" FAB (`NewPostFab.tsx`) is now a small dropdown — "New post" (unchanged, `/brief`) and "New Slide Deck" (`/deck-brief`, new).
  - **A real gap surfaced during build, not yet fixed:** the deck wizard hardcodes `designMode: 'GENERATE'` (Path B) for every slide — `Deck` has no `templateId` column and `generateDeck.ts`'s per-slide `createPendingDraft` call passes none, so a Path A (`TEMPLATE`) deck would 500 at approval (`TemplateNotFoundError` uncaught by the approve route's error mapping). Path A support for decks (a `Deck.templateId` column + wiring through `generateDeck.ts`) is a deliberate follow-up, not a silent bug — flagged here so it isn't rediscovered as a surprise.
  - Gates: tsc clean, 13/13 tasks built and committed on `specclaw/slide-deck-generation` (not yet merged to `main`). Unit: 50/50 new deck-specific cases green (`tests/unit/deck/outline.test.ts`, `tests/unit/deck/generateDeck.test.ts`, `tests/unit/deckActions.test.ts`, extended `visibility.test.ts` + `export/pptx.test.ts`) — confirmed by running with `NODE_ENV` explicitly unset (see the caveat below). New E2E suite `deck-generation.test.ts` (§U, 4 cases) **confirmed green 2026-08-04** against a clean live stack (killed the stale polluted-`NODE_ENV` test server first). That first live run caught two real test-only bugs, both fixed (commit `9c855ca`): the test assumed `Deck.proposedOutline` was a bare array (it's `{slides:[...]}` everywhere in the real API — route.ts/useDeckWizard.ts/types.ts all agree), and three assertions compared full presigned export URLs instead of the bare object path — since presigned URLs are re-signed on every `GET` (H10), that made `waitForSlideExportChange` return on its first poll regardless of real state (same latent bug also caused the flaky case in `pptx-export.test.ts`'s §T suite, fixed too). Full-catalog run also hit 8 pre-existing, unrelated browser-login `next dev`/Turbopack HMR fetch-abort flakes (`ui.test.ts`, `campaign-scheduling.test.ts`, `settings-claude-token.test.ts` — none touched by this branch's diff, reproduces on a clean server independent of this change) — not a regression, not fixed here. **⚠️ Deploy:** one new migration (`20260804024343_deck_schema`); no new env vars.
  - **🧠 Sandbox gotcha worth remembering:** this dev container has `NODE_ENV=production` exported at the shell level (for some other unrelated purpose). Both `vitest` and `next dev` inherit it, and `src/lib/env.ts`'s "refuse to start in production with placeholder MinIO/token secrets" guard then fires against `.env.test`'s intentionally-placeholder values — silently zeroing out large swaths of the unit suite (test files fail to even load, reporting 0 tests instead of a clear error) and 500ing every route on a `next dev` test server. Always run test commands as `env -u NODE_ENV ...` (or equivalent) in this environment, and check `ps aux` for a stale test-server process carrying the same pollution before assuming a fresh one will differ.

- **✅ 2026-08-03 — PowerPoint (.pptx) slide export** (specclaw change `pptx-slide-export`; spec/design/tasks in `.specclaw/changes/pptx-slide-export/`). New `POST /api/drafts/[id]/export/pptx`: image-backed single-slide deck (`pptxgenjs`) — the draft's already-rendered export PNG placed full-bleed as the slide background, slide dimensions in inches matching the post's own aspect ratio (`dimensionsFor()` ÷ 96dpi — SQUARE/PORTRAIT/STORY, never letterboxed). Generated on-demand per request and **not persisted** (no schema change, no new migration) — a deterministic re-packaging of an asset that's already stored. Reuses the existing render-if-missing side effect: `POST /api/generate/export`'s "no `exportUrl` yet → render → upload → mark `EXPORTED`" block is now the shared `renderAndStoreExport()` export, called identically by both routes (pure extraction, no behavior change to the existing route). New `src/lib/export/pptx.ts` (`buildPptxBuffer`, `pptxFilename`) is the pure, unit-tested surface; the route is a thin wrapper matching the project's existing route-thinness convention. UI: "Export as PPTX" button on the draft review page and an icon action on the library `PostCard` (visible once `exportUrl` is set); both use a new shared `src/lib/download.ts` (`downloadBlobFrom`) since the response is binary and `apiFetch` always parses JSON — same fetch→blob→`<a download>` technique `ImageLightbox.tsx` already used for cross-origin MinIO downloads, just against a same-origin route this time.
  - **Native/editable PowerPoint shapes and multi-slide campaign decks are explicitly out of scope (v1)** — noted as v2 candidates in the proposal; decomposing arbitrary CSS into PPTX shape geometry is lossy/fragile, and the per-draft route shape was chosen so a future bulk (campaign-deck) version can loop over drafts without reworking this route.
  - Gates: tsc clean, lint clean (0 errors, 7 pre-existing warnings — matches the long-standing baseline), **364/364 unit** (10 new pptx-builder cases), production build green. New E2E suite `pptx-export.test.ts` (§T, 4 cases — happy path/no-re-render, render-if-missing, 422 no-HTML, 404 cross-team) added to the catalog; not yet run against a live MOCK_AI/MOCK_PUPPETEER/test-DB stack on this machine (`.env.test` wasn't present — pre-existing local gap, unrelated to this change) — run `npm run test:e2e:mock` before merging to confirm green. **⚠️ Deploy:** no migration, no new env vars — a plain redeploy picks up the new dependency (`pptxgenjs`) and route.
- **✅ 2026-07-28 (later) — editing the copy silently broke the whole draft: one status write, five symptoms.** Reported as "delete the copy completely and the Refine design option disappears; regenerate the copy afterwards and some posts say draft not ready — fix it so we don't have to regenerate every time it breaks."
  - **Root cause — a single line.** `PATCH /api/drafts/[id]` (the copy editor's save) flipped an `EXPORTED` draft to **`IN_PROGRESS`** on any copy edit, to mean "the export is now stale". But `IN_PROGRESS` in this codebase means **"generation is running right now"**, and every consumer read it that way. One write, five downstream failures: the draft **dropped out of the library** (it filters on `EXPORTED`), **Refine design + Edit inline vanished** (both gate on `ready = EXPORTED || PUBLISHED`), **Regenerate copy answered 409 `'Draft is not ready for copy regeneration'`** — the reported "draft not ready" — clearing the copy entirely left the page on an **eternal "Writing the copy…" skeleton** with no field to type into (`copyPending = isGenerating && !copyText`), and **15 minutes later the lazy sweep marked the draft `FAILED`** with "Generation was interrupted". Nothing was ever generating, so the only escape was regenerating the whole post.
  - **Fix, three parts.** (1) The PATCH **no longer touches `status`** — copy and design are independent artifacts (the copy is the post caption, the export is the rendered image); `regenerate-copy` already left status alone (§Q TC-ASYNC-02), so this handler was the straggler. A "your export predates this copy" nudge belongs in the UI, not in the status field. (2) `copyPending` also requires **no `htmlContent`** — generation writes copy _before_ the design exists, so empty copy on a draft that already **has** a design is a deliberate deletion and must render the editable field, never an uneditable skeleton. (3) Already-stranded drafts are **healed, not abandoned**: new pure planner **`src/lib/drafts/recovery.ts`** (`planDraftRecovery`, unit-tested without a DB — same split as `inlineEdit.ts`) restores them to `EXPORTED` on read, and migration **`20260728120000_heal_copy_edit_clobbered_drafts`** repairs them in bulk because a draft missing from the library can't be opened, so the read-time heal would never fire for it.
  - **Why the heal is safe, and why it isn't time-gated.** `exportUrl` is only ever written **together with** `status: 'EXPORTED'` and `currentRevisionNumber` in one transaction (`finalizeDraftV1`, `/api/generate/export`, `commitDraftRevision`, revision restore — verified). So a draft carrying export + html + revision pointer had **finished** generating: it's a live post with a clobbered status, not a broken run. A genuinely interrupted generation has `exportUrl` NULL and is left alone. The heal therefore needs no 15-min wait (there is no run to wait for) and it **suppresses** the generation sweep — a clobbered status must be restored, never failed. Every write guards on the observed value, so a concurrent transition always wins, and no branch touches draft **content**.
  - **🧠 Lesson: don't overload a state field with a second meaning.** `status` answers "is a generation running"; "is the export stale relative to the copy" is a _different_ question and needed its own answer. Reusing the enum made five unrelated consumers wrong at once, and each looked like its own bug. Watch for this wherever one field is written to signal something it doesn't name.
  - Gates: tsc clean, lint clean (0 errors, 7 pre-existing warnings), **335/335 unit** (13 new `draftRecovery` cases), E2E §Q + new **TC-ASYNC-10** green. **⚠️ Deploy:** one new migration — a redeploy applies it (PR #39 entrypoint). No new env vars.
- **✅ 2026-07-28 — team admins were locked out of `/admin/brandkits`, and Sinhala text now renders in generated posts (pushed straight to `main`).** Two unrelated fixes in one push.
  - **Brand-kit access (bug in `image.png`):** `src/app/(app)/admin/layout.tsx` gated **every** `/admin/*` page on the **global** `Role` (`hasRole(user.role, 'admin')`). But brand kits are **team-scoped** — the sidebar shows the link on `isTeamAdmin` and the API uses `withTeamAdmin`, both of which a **team admin** (whose _global_ role is `EDITOR`) passes. So a team admin saw the Brandkits link, clicked it, and hit the "Requires admin" panel even though `/api/admin/brandkits` would serve them. **Fix:** the layout now gates on **team admin (or super admin)** via `resolveTeamForServerComponent()` (`teamRole === 'ADMIN' || isSuperAdmin`) — exactly the check `/team/layout.tsx` already used (whose comment even _claimed_ it mirrored `admin/layout.tsx`; in fact `admin/layout.tsx` was the straggler still on the global role). `/admin/users` + `/admin/teams` self-gate on super-admin **inside their own pages**, so loosening the shared layout does not widen their access; team **editors** remain blocked.
  - **Sinhala fonts (always-available fallback):** Alpine's `chromium` ships Latin-only fonts, so Sinhala (සිංහල) copy rasterized as tofu boxes on prod. **`Dockerfile` runner stage now installs `font-noto-sinhala`** (Alpine community, same repo as chromium) OS-wide — Chromium's fontconfig fallback then covers Sinhala codepoints **automatically**, with no CSS/`@import` needed in the generated HTML. Belt-and-braces prompt guidance added: a shared **`SCRIPT_SUPPORT_NOTE`** (in `prompts/shared.ts`) wired into Path A / Path B / refine tells the design agent to reproduce non-Latin glyphs exactly (never romanize/drop) and that `"Noto Sans Sinhala"` is available; `PROMPT_VERSION` → `2026-07-28.1`. AI copy needed no change — Claude already writes Sinhala on request; rendering was the missing half. Local Windows dev already rendered Sinhala via Nirmala UI. **⚠️ Deploy:** needs an **image rebuild** for the font (baked into the runner image) — a `main` redeploy does this; no migration, no new env vars.
  - Gates: tsc clean, lint clean, **322/322 unit**. No schema change.
- **✅ RESOLVED 2026-07-27 — the deploy pipeline never ran migrations (PR #39 `16e1a069`).** Discovered while chasing "can't create a team named Hearts Academy on prod". PR #38 was merged AND deployed green, yet the symptom persisted: `Dockerfile` ended at `CMD ["node","server.js"]` with **no entrypoint and no `prisma migrate deploy`** anywhere in the image, `docker-compose.yml`, or `docker-publish.yml`. Every past migration had reached prod only because a human ran it by hand. So prod kept the old `Team_name_key` and `create` still tripped P2002 → the same 409 the fix removed. **New code against an old schema.**
  - **Fix:** **`docker-entrypoint.sh`** runs `prisma migrate deploy` then `exec "$@"`; `ENTRYPOINT` added, `CMD` unchanged. Both Coolify resources boot through it (the scheduler overrides `command:`, not `entrypoint:`) — `migrate deploy` takes a Postgres **advisory lock**, so simultaneous boots serialize. Fails loud (`set -e`): a bad migration fails the deploy and the previous container keeps serving. Escape hatch `SKIP_MIGRATIONS=1`. Needed one extra layer, **`COPY --from=builder /app/node_modules/prisma`** (prisma is a devDependency the runner's `npm ci --omit=dev` skips; the already-copied `@prisma` scope carries the linux-musl `schema-engine`), invoked as `node node_modules/prisma/build/index.js` — no `npx`, so no per-boot registry download. Plus `.gitattributes` `*.sh text eol=lf`, because a Windows clone would otherwise give the entrypoint a CRLF shebang that Linux CI never reproduces.
  - **Confirmed working in prod:** the migration applied on boot and the **Hearts Academy team was created minutes later** — the name that had been permanently reserved. `Team_name_active_key` now exists.
  - **⚠️ The "⚠️ Deploy: run `npx prisma migrate deploy`" notes throughout this file are now obsolete** — a redeploy applies migrations itself. Treat them as historical.
  - **🧠 Lesson worth keeping:** a merged + deployed + CI-green fix can still be completely inert. When a fix "doesn't work in prod", check the schema half of the release before re-reading the code.
- **✅ RESOLVED 2026-07-27 — the brief wizard blocked generation with no COPY provider (PR #40 `21f95035`).** Reported as "if there is no team copy provider, the brief can't be generated" — correct, and PR #30 had only fixed the **server**. The wizard kept its own rule: Generate was `disabled={!copyProviderKey}` (`brief/page.tsx`), with a matching early return (`useBriefWizard.ts`) and amber warning (`ReviewStep.tsx`). With no COPY row `pickDefault()` returns `''`, so the button was **permanently dead** and the route's CLI default was unreachable from the UI.
  - **Fix:** deleted the client's separate rule. **`canSubmitBrief()`** now sits beside `resolveBriefCopyKey` in `src/lib/brief/copyProvider.ts`, defined as the negation of that function's error case — the button is live exactly when `POST /api/briefs` would accept the submission, so the two cannot drift again. API mode still gates (a missing key there really is a 400). Also sends `copyProviderKey: undefined` rather than `''`, and folds `/api/me`'s loading state into `providersLoaded` so the warning doesn't flash before `cliMode` is known.
  - **Why a green E2E suite missed it:** `scripts/seed-cli-provider.mjs` seeds a placeholder `cli` COPY row (`isDefault`) on the local Bistec team, so the gate always found something; and `.env.test` runs `DESIGN_PROVIDER=claude-html` — API mode, where the gate is **correct**. It only surfaces on a team built without that legacy row, which is exactly what the prod teams are. **Beware this class of masking whenever a seed script exists to make a gate pass.**
  - Design generation needed no change — it is selected by `DESIGN_PROVIDER`, not an `AvailableProvider` row (there is no DESIGN slot, only COPY and IMAGE), so it already defaults to `claude -p`.
- **2026-07-26 session (see `docs/handoff.md`):** the prod **Bistec** team's brand kits were rebuilt via the `/admin/brandkits` UI — **Bistec** (default) + **Hearts Academy**, each with colours/fonts/3 labelled logos/active voice prompt, verified against the local DB. **One gap:** Hearts Academy's 1.9 MB inline-base64 "Hearts Talk" template was **skipped** (add later via a direct API POST or a slim URL-referencing rebuild). Note these are brand **kits under the Bistec team** — a separate _Hearts Academy team_ is a different tenant and starts with zero kits.
- **Everything is merged to `main` now (2026-07-24):** the prod-fix branch (**PR #30** `4e8e6e3e` "Fix/prod render and cli copy", merged 2026-07-23), **PR #35** `d01ac4d2` multiple-brandkit-logos, and **PR #36** `3dcac485` draft-inline-edit (both merged 2026-07-24). All three branches can be deleted. The GHCR image built after these merges (`.github/workflows/docker-publish.yml` → `ghcr.io/bistec-oss/studio:latest` + `:sha`, run green ~06:04 2026-07-24) contains **all** of them.
- **Deploy topology (confirmed from `.github/workflows/docker-publish.yml`):** the build job pushes to GHCR **and then calls the Coolify deploy API** for both resource UUIDs (added 2026-07-24, commit `1116189`) — so a green `main` build **does** now redeploy prod. Coolify runs **two resources off the one image** (app = default CMD; scheduler = CMD override `node dist/scheduler/worker.js`, per `docker-compose.yml`). **A redeploy now also applies migrations, via the PR #39 entrypoint — it did not before 2026-07-27.** Verify a deploy at Coolify → resource → Deployments (running tag/SHA).
- **Prod redeployed and B3/B5 are FIXED (2026-07-24, see [`docs/prod-e2e-findings-2026-07-24.md`](docs/prod-e2e-findings-2026-07-24.md)).** The auto-redeploy webhook worked; prod is on the post-fix image. **B3 (HTML→PNG render) is fixed** — full Path B generation reached **EXPORTED in ~195s** with a real gpt-image background, and all three async draft actions + from-image vision + both new features passed. **B5** (exported PNGs unreachable in-browser — presigned EXPORTS URLs pointed at the internal Coolify MinIO container host) was found in that run and fixed by **PR #37** `fb8216c3`, merged the same day.
  - **🟠 B4 (still open) — scheduled-gen worker not running.** A past-due HOLD entry stayed PENDING through >3 poll cycles (185s), `retryCount:0`, never claimed. The app resource is demonstrably redeployed, so this is specifically the **scheduler resource not running** (or not pointed at this DB). Fix = ensure Coolify runs the `docker-compose.yml` `scheduler` service (same image, CMD `node dist/scheduler/worker.js`, same `.env`), and confirm webhook UUID `warr96qhvzrie5ndwv8oteeu` targets a live worker. B4 only disables **scheduled generation**; manual generation works.
- **⚙️ Config gap — fully closed as of PR #40:** CLI-mode teams needed a per-team `cli` COPY provider or generation was impossible. PR #30 removed the **server** requirement; **PR #40 removed the matching client gate** (see above). A team now needs **no COPY provider at all** in CLI mode — copy runs `claude -p` on the OAuth chain. `scripts/seed-cli-provider.mjs` is therefore **legacy**: harmless (an explicit `cli` row still resolves via `instantiateCopyProvider`'s `"cli"` case) but no longer required, and actively misleading during testing because it masks gate bugs. One `cli` provider (`cli-1784786367943`) was registered on Claude Testing during earlier testing.
  - **`/team` → AI Providers now hides the COPY slot in CLI mode (2026-07-28).** Offering it invited admins to register a key that would never be used. In CLI mode the section explains that copy runs on the OAuth chain, `Register Provider` becomes **`Register image key`**, and the slot toggle disappears (locked to IMAGE). **Two deliberate carve-outs:** the COPY section still appears when the team **already has** a COPY row — otherwise a legacy row would be invisible and undeletable, so it's shown with "Not used in CLI mode — safe to delete" — and in **API mode** the slot is unchanged, because there a COPY provider is genuinely required.
- **🔑 CLI-mode credential chain (verified in code 2026-07-27 — supersedes the stale note in the per-user-token section below):** exactly **two tiers, personal → team, with NO env or dev-session fallback** (`src/lib/agent/claudeAuth.ts` header + `resolveClaudeAuth` in `userToken.ts`). A personal `UserClaudeToken` is used only when `status === 'ACTIVE'` and the user isn't disabled; a _rejected_ personal token is marked INVALID and retried **once** against the team token. No credential at all ⇒ `runClaudeCliOnce` hard-fails. **The asymmetry that bites:** interactive routes pass the acting user (`withClaudeAuth(user.userId, user.teamId, …)`, `startBackgroundGeneration(draftId, userId, teamId)`), but the **scheduler and MCP pass `null`** (`generationRunner.ts`, `mcp/tools/generate.ts`) — they have **no personal tier** and need a _team_ token.
- **🟠 Neither prod team has a team Claude token** (`/api/team/claude-token` → `{connected:false}` for both Bistec and Hearts Academy, checked 2026-07-27). Interactive generation works off the acting user's personal token, but **scheduled generation and MCP will hard-fail on credentials for both teams** — a second blocker behind B4, so fixing the scheduler resource alone won't make scheduled posts work. Set a team token at `/team`. (The 2026-07-16 "scheduled generation runtime-verified in CLI mode" note predates team tenancy, when a shared env token still caught this.)
- **🟠 Both prod IMAGE providers are `isEnabled: true` but `isDefault: false`.** `resolveImageProvider` tier 3 requires `isDefault`, so a teammate **without** a personal OpenAI key silently gets no AI background — generation still completes (it never throws; design falls back to CSS/SVG). Mark the IMAGE row default on each team to fix.
- **Prompt-injection review (analysis only):** feasible on chat/enhance surfaces; blast radius narrow (content manipulation; tenancy holds, no stored-XSS sink). Sharpest vector = CLI-mode vision's `--allowedTools Read` (server file disclosure), LIVE on prod since the CLI switch. PR #30 landed the prompt-level hardening (`fenceUntrusted`/`UNTRUSTED_CONTENT_GUARD` + CLI-vision "read only listed files"); residual follow-up = OS-level sandbox for the CLI `Read` tool. See the security doc.
- **🏗️ Prod data built 2026-07-27 (Hearts Academy team — real data, not test data, do NOT wipe):**
  - **Team "Hearts Academy"** `cms3izrx90000t5ijbsepjstm` — the creation that proved PR #39.
  - **Brand kit "Hearts Academy"** `cms3jg09p000at5ijivpi3zhe`, **team default**: 6 colours, Poppins + JetBrains Mono, 3 labelled logos (primary "BISTEC Hearts Academy"), voice prompt. Copied from the Bistec team's kit, but with its **own** uploaded logo objects under `brand-kits/cms3jg09p000at5ijivpi3zhe/` — no cross-team object references. Source filenames preserved deliberately: the voice prompt says "the file names identify them".
  - **Kit voice prompt → v4** (every earlier version preserved under History; VOICE + PLATFORM carried over verbatim throughout). Brand-level corrections only, so the kit isn't over-fitted to one campaign. **v2:** logo order corrected to **Hearts Academy left, BISTEC Global right** (v1 had it backwards vs the actual published creative); canvas became a **teal-to-green gradient with a faint white graph-grid**, navy demoted to accent bands/panels; **photographic imagery permitted** (v1 said schematic illustration only) with a hard rule that type must never overlap it; contact footer documented. **v3:** the logo lockup respecified **geometrically** — a compact, horizontally centred pill that **hugs** the logos (~45–60% canvas width, never full-bleed), both marks **close together in the middle** reading as one unit. **v4:** logos are sized by **wordmark CAP HEIGHT, not overall mark height** — the Hearts lockup is _stacked_, so matching total heights shrinks its lettering; the capitals in "Hearts Academy" must match those in "BISTEC", making the Hearts mark taller overall, which is correct.
  - **Campaign "Industry Readiness Programme (IRP)"** `cms3kkxqt0011t5ijgd9urims` — kit override Hearts Academy, standalone, no default tone. **Briefing → v3** carries: the **Editorial Split** layout (type and imagery never overlap, image in its own ~40% band); a **⚠️ FACTS — DO NOT INVENT** rule that outranks styling (no intake/deadline/duration/cohort/placement claims unless that post's brief states them, else an open CTA); **NAMING — "the programme must be NAMED, not described"** (v2: the headline _must_ contain "INDUSTRY READINESS PROGRAMME" or "IRP"; substitute names like "Real Industrial Training" are forbidden as headlines; descriptors demoted to a smaller subline); and **EDGE TREATMENT** (v3: the type/image boundary is never a crisp vertical edge — mask the image's left edge with a transparent→opaque gradient across ~10–15% of canvas width, blur the boundary, no line/border/stroke/shadow — and the fade belongs to the **image, not the text**, so legibility is never traded away). Reference creative attached as campaign document `irp-reference.png` (grounds the briefing assistant via vision only — campaign docs never enter generation prompts).
  - **⚠️ Two unverified facts in that briefing:** the contact block (`076-8030-944`, `heartsacademy@bistecglobal.com`, `www.hearts.academy`) was read off the reference image, and the "undergraduates" audience is inferred from its "Are you an Undergraduate?" hook. Confirm both before publishing.
  - **✅ VERIFIED end-to-end 2026-07-28** — first real IRP post, draft `cms3m2fvk00055tlsv465otld`, 1:1 Path B, **EXPORTED** (copy at ~42s, full run ~5 min). Proves in one shot: **PR #40** (Generate button live on a team with **zero** COPY providers — no amber warning), **copy via `claude -p`** on the personal OAuth token with no COPY provider, **the kit voice prompt** (then at v2 — Hearts Academy logo LEFT / BISTEC Global right, teal→green gradient + faint graph-grid, photographic imagery), the **Editorial Split** (type left, photo in its own right band, **zero overlap**), a real **gpt-image background**, and **PR #37/B5** (presigned export URL on the public MinIO host, fetched 200 from outside the cluster). The **FACTS — DO NOT INVENT** rule held: CTA is "Apply Now"/"Applications are open" with **no month, deadline, or fabricated numbers** in image or caption; copy names "Industry Readiness Programme" in full, uses #IRP, and gets "BISTEC Hearts Academy" exactly right.
  - **Three rounds of design fixes, each verified by a real regeneration** (drafts `cms3m2fvk00055tlsv465otld` → `cms3meigo0009zb6ejz40bh7p` → `cms3mrfew00099tx3wkz5972n`, all EXPORTED, ~5 min each): **(1)** the headline said "REAL INDUSTRIAL TRAINING" instead of naming the programme → fixed by briefing v2 + kit v3; **(2)** the Hearts logo rendered much smaller than BISTEC Global → fixed by kit v4's cap-height rule; **(3)** the type/image boundary read as a harsh seam → fixed by briefing v3's EDGE TREATMENT. All three now render correctly.
  - **🧠 The pattern behind every one of those misses — worth applying to any future prompt work.** Each came from an **adjectival** instruction and each fix came from replacing it with something **measurable**:

    | Didn't stick (adjective)              | Stuck (measurable)                                                |
    | ------------------------------------- | ----------------------------------------------------------------- |
    | "IRP is fine on-image" (a permission) | "the headline MUST contain INDUSTRY READINESS PROGRAMME or IRP"   |
    | "similar visual heights"              | "the capitals in 'Hearts Academy' match the capitals in 'BISTEC'" |
    | "generous clear space"                | "pill width ~45–60% of canvas, gap ≈ one or two wordmark letters" |
    | (no rule at all for the seam)         | "transparent→opaque mask across ~10–15% of canvas width"          |

    Also: a thing that must appear **in the rendered image** has to be a requirement **on the image**, not a caption rule. The first miss happened because the naming rule was scoped to the caption and the model obeyed it exactly.

  - **🟠 One open drift:** the latest render's canvas reads **navy/blue dominant** rather than the kit's "teal-to-green gradient is the primary canvas", with green reduced to accents. Same adjectival weakness as the above — the rule has no proportion attached. Fix by giving it a number if the green-forward look matters.
- **📋 Prod test status** (against the **Claude Testing** team; test data still KEPT — IDs in the 2026-07-23/24 findings docs). **Passed 2026-07-24:** full generation copy+design → EXPORTED, all 3 async draft actions, from-image final render, and both new features (multiple brand-kit logos, draft inline-edit). **Passed 2026-07-27:** team-name reuse after delete (creating "Hearts Academy" — a previously-reserved name — succeeded). **Still outstanding:** (a) worker-run **scheduled generation** — blocked on **B4** _and_ the missing team Claude token above; (b) **publish** — still needs social credentials; (c) visual confirmation that an exported thumbnail now renders in the library (**B5**/PR #37 deployed but not re-verified); ~~(d) a generation on a team with **no COPY provider**, to confirm PR #40's live Generate button end to end.~~ **(d) ✅ done 2026-07-28** — the IRP post above. **Wipe the kept Claude Testing data after (a)–(c)** — but not the Hearts Academy data above.

**✅ Team tenancy — full multi-tenant rework — MERGED TO `main` 2026-07-22** (squash-merged as PR #29, commit `2a118a73` "Feature/team tenancy (#29)"; the `feature/team-tenancy` branch still exists locally but `main` is now the source of truth; spec `docs/superpowers/specs/2026-07-21-team-tenancy-design.md`, plan `docs/superpowers/plans/2026-07-21-team-tenancy.md`). **Now hosted in production at `https://studio.bistecglobal.com`.**

- **What it is.** bistec-studio converts from a single shared org into partitioned **Teams**. 37 commits, built task-by-task via subagent-driven development with a per-task implementer/reviewer split (`.superpowers/sdd/progress.md` is the ledger).
- **Architecture.** `teamId` added to 12 models (Project, Campaign, BrandKit, Brief, Draft, Post, ScheduledGeneration, BriefDraft, CampaignDocument, BrandKitDocument, AvailableProvider, ChannelToken). New `withTeamAuth`/`withTeamAdmin` route wrappers resolve the active team from a membership-validated `bistec-active-team` cookie — an unpicked team on a multi-team user returns 409 `{code:'team-choice-required'}`, and the client (`apiFetch`) redirects to `/choose-team`. Visibility is centralized in `src/lib/authz/visibility.ts` (spec D6): an editor sees their own items **plus** anything under a campaign (the campaign is the sharing container); a team admin sees the whole team; cross-team is always a 404, never a 403 (no existence leak). `TeamRole` is a closed `ADMIN | EDITOR` enum compared directly (not hierarchical); super admins pass every team gate.
- **Credentials are all in-DB now, nothing in `.env`.** Personal Claude token + personal OpenAI key at **`/settings`**; team Claude token, AI providers, social channels, and MCP/ACP API keys at **`/team`** (team admins only); teams + memberships at **`/admin/teams`** (super admins only). Resolution order is personal → team → fail/skip (never throws for OpenAI — background images just skip). MCP/ACP auth uses hashed `bstk_`-prefixed `ApiKey` rows (plaintext shown once at creation, never stored); `MCP_API_KEY` is now the _presented_ credential, not a bypass list.
- **⚠️ Deploy (next machine):** `npm install` (one new dep, `@radix-ui/react-dropdown-menu`) → plain `npx prisma migrate deploy` — **two new migrations**, `20260721090042_team_tenancy_a` and `20260721120000_team_tenancy_b`. Migration B **embeds the backfill** (creates a default team "Bistec", derives memberships from existing user roles, absorbs every pre-team row) ahead of its `SET NOT NULL`/constraint changes, so there is **no separate script step**. `scripts/migrate-to-teams.mjs --dry-run` still exists but is inspection-only now.
- **8 env vars removed** — delete from the other machine's `.env` after deploying: `OPENAI_API_KEY`, `CLAUDE_CODE_OAUTH_TOKEN`, `BISTEC_API_KEYS`, `BISTEC_ADMIN_API_KEYS`, `LINKEDIN_ACCESS_TOKEN`, `LINKEDIN_ORGANIZATION_ID`, `INSTAGRAM_ACCESS_TOKEN`, `INSTAGRAM_BUSINESS_ACCOUNT_ID`. `ANTHROPIC_API_KEY` (API mode) and `MCP_API_KEY` (the key a stdio MCP client presents) survive. **Post-deploy ops:** set team credentials at `/team`, connect personal tokens at `/settings`, re-issue MCP/ACP keys at `/team` (old env-list keys no longer work).
- **Constraint swaps:** `AvailableProvider` unique moved to `(teamId, slot, providerKey)`; `ChannelToken` unique moved to `(teamId, channel)`.
- **Old surfaces removed:** the old `/admin/settings` page is gone (folded into `/team`); the old `withAdmin` wrapper is deleted (plain `withAuth` now survives only for `/api/me/*` + auth-adjacent + owner-only brief-draft routes). Sign-out and other unrelated UI are unchanged.
- **E2E:** new suites `tests/e2e/team-isolation.test.ts` (§R, 19 cases — cross-tenant isolation guardrail incl. foreign template/FK injection + kit-less-team fallback) and `tests/e2e/team-settings.test.ts` (§S, 7 cases); seed now creates two teams via `scripts/seed-teams.mjs` (Bistec + ClientX, `clientx.admin`/`BistecStudio2026!`) plus a kit-less third team for the fallback guardrail; `TC-AGUI-06` rewritten to D6 semantics; §J ACP cases mint real `ApiKey` rows instead of env keys. Full catalog at HEAD (2026-07-22, post TC-REG-H7a de-flake): **171 passed / 4 skipped / 0 failed** (175 total, 0 flaky); unit **264/264**; production build green.
- **16 cross-tenant gaps found and fixed within the branch, none ever reached `main`:** the Task 19 isolation suite flushed out 8 real IDOR-shaped holes (campaign/project/brand-kit get-by-id, queue subtree, draft PATCH, `resolveCopyProvider`); a 9th (unscoped Anthropic key resolution) surfaced as a same-day blocker (Task 19b); and the **final whole-branch review** (`.superpowers/sdd/final-review.md`) found the last family — brand kits/templates weren't fully inside the tenant boundary (`resolveBrandKit` default tier, MCP brandkit tools cross-tenant R/W, foreign template/reference-template/FK injection on generation + campaign/project routes, queue template bypass, plus a live duplicate leak in the API-mode agent tool loop) — all closed in one commit with matching isolation tests, re-verified by a fresh full E2E run and a final reviewer verdict of **Ready to merge: Yes**. Full list in `.superpowers/sdd/progress.md`.
- **User review-pass fixes (2026-07-21 evening, on-branch):** dashboard "Drafts ready"/"Posts published" KPIs now use the same D6 visibility helpers as the listings (an editor saw team-wide counts their own library contradicted — e.g. 13 vs 2); new near-opaque `.glass-popover` surface for floating menus (the team-switcher dropdown was see-through over nav text — `.glass-panel`'s 60% alpha is for in-flow panels only); docs de-stale sweep (social guide → `/team`, e2e plan + CI workflow dropped the deleted env-list API keys); this machine's `.env` cleaned to infra-only with accurate comments.
- **Status: MERGED to `main` 2026-07-22 (PR #29, commit `2a118a73`) and deployed to production at `https://studio.bistecglobal.com`.** The earlier "parked on branch, merge only on go-ahead" directive has been satisfied — the go-ahead was given and the merge landed. The `feature/team-tenancy` branch still exists locally and can be cleaned up. The deploy steps above apply to any additional machine/environment.
- Gates: tsc, lint, **264/264 unit**, full mock E2E 170/4/0, production build.

**✅ UI clarity pass — 2026-07-20** (UI-only; see `docs/handoff.md` top section):

- **Select chevron fix:** `.glass-input` used the `background` **shorthand**, wiping the Select's chevron `background-image` in dark mode + on focus — now `background-color`. Never use the shorthand in classes that coexist with bg-image utilities.
- **Sidebar sections** (`AppShell.tsx`): labeled groups **Create** (Dashboard, Library) / **Organize** (Projects, Campaigns) / **Admin** (Brandkits, Users — role-gated, hidden when empty); Settings joined Sign out in the bottom-pinned area.
- **Campaigns page grouped by project:** per-project sections (linked heading + count), Standalone group last; brand-kit chips get a Palette icon, tone chips a MessageCircle icon (also on project cards); campaign detail breadcrumb shows `Projects / <project> / <campaign>` when applicable. No API/schema changes.
- **Env hygiene:** `ANTHROPIC_API_KEY` removed from this machine's `.env` and commented out in `.env.example` (CLI mode never uses it; API-mode-only). `docs/cold-start.md` §2 updated.
- Gates: tsc, lint (0 errors, 7 pre-existing warnings), 180/180 unit. No migrations, no new env vars.

**✅ Async draft actions (202+poll) + brand-kit logo URL hygiene — 2026-07-17** (specclaw change `async-draft-actions`; see `docs/handoff.md` top section):

- **Incident root cause:** a 136,050-char base64 `logoUrl` on the Hearts Academy kit entered design prompts **twice** (`buildBrandKitSystemContext` + the `feedToAI` artifact list in `pathB.ts`) → 277k-char prompt → 300s CLI timeout on regenerate.
- **Part A — the three draft actions are now async (F1 pattern):** regenerate-design / regenerate-copy / refine validate synchronously, **atomically claim** the new `Draft.pendingAction` (second action → 409), return **202 `{ok:true}`**, and run fire-and-forget (`src/lib/drafts/draftActions.ts`). `GET /api/drafts/[id]` returns `pendingAction`/`pendingActionError`/`conflict` for the 4s poll; 15-min stale sweep at the `recoverIfStuck` site; restore 409s while an action runs. `Draft.status` untouched (regenerate-copy's IN_PROGRESS flip removed; it 409s on non-EXPORTED/PUBLISHED). Override stays sync. Client captures undo snapshots BEFORE firing (`onActionStarted`); overlay/disabled states, not skeletons. **Old sync response shapes are gone** (breaking only for in-repo callers, all updated; scheduler/MCP/ACP untouched).
- **Part B — logos are always URLs:** data-URI guards in `buildBrandKitSystemContext` + pathB artifact filter (`PROMPT_VERSION` → `2026-07-17.1`); non-http(s) `logoUrl` → 400 at both brandkit routes + MCP `create_brand_kit`; `seed-hearts-talk.mjs` uploads instead of embedding; new idempotent `scripts/fix-data-uri-logos.mjs` (`--dry-run`). **Data fix applied on this machine:** Hearts Academy `logoUrl` → transparent PNG in the brand-kits bucket, mislabeled 136k artifact replaced ("BISTEC Hearts Academy", feedToAI) — 0 data-URIs remain locally.
- **TC-REG-H7a rewritten** to the single-flight contract (10 sequential refines → contiguous 2..11; parallel fire → every request 202-or-409-never-500, one revision per 202 winner — **de-flaked 2026-07-22:** the old "exactly one 202, rest 409" assertion was timing-dependent under the instant mocks and flaked, so it now asserts the mock-stable integrity invariant instead); §Q suite (9 cases — **10 as of 2026-07-28**, TC-ASYNC-10 guards the copy-edit status clobber) + TC-BK-09 added; `agui-refinement` rewritten async. Also fixed 3 pre-existing lint errors in `scripts/export-posts.mjs` and restored `BISTEC_API_KEYS` to `.env.test`.
- **Same-day addition — brand-kit assistant source documents:** the assistant chat gets campaign-style uploads — new `BrandKitDocument` table (migration `20260717114732`) + `GET/POST/DELETE /api/admin/brandkits/[id]/documents(/[docId])` (5/kit cap, images+PDF/DOCX/TXT/MD, private docs bucket, manual delete). Documents ground the chat (∪ feedToAI artifacts, doc-images first, cap 6 — `collectBrandKitGrounding`) but are **never artifacts and never enter generation prompts**. Panel gains the "Source documents & images (N/5)" block. 180/180 unit; targeted E2E 14/14.
- **✅ Live-verified same day (real dev server, CLI Claude, no mocks):** brief-draft autosave → Resume (full rehydration incl. step/campaign/kit) → Discard, all in a real browser; async regenerate-copy 202 → `pendingAction` → new copy → Undo restore; async refine proven by six real revisions the same afternoon; brand-kit doc upload → live chat quoted a marker from the doc → delete; Hearts-kit prompts back to ~5k chars (vs 141k/277k incident), regenerate-design 41s vs the 300s timeout. (Testing note: DevTools-protocol `fill` doesn't fire React `onChange` on textareas — use real keystrokes when browser-testing autosave.) **Sharpened 2026-07-28 while browser-testing the brief wizard:** the usual workaround — React's native `value` setter plus a bubbling `input` event — **also fails** here (the `<input>` topic field registered, the `<textarea>` did not: DOM value 425 chars, counter still "0 chars", Continue still disabled). What works is cheap: set the value however you like, then send **one real keystroke** into the focused field — React's `onChange` fires with the _entire_ DOM value, so a single space picks up all 400+ characters at once. Verify via the component's own state, not the DOM (`el.value`), which lies.
- **Hearts Academy kit rebranded (this machine's data, not code):** voice prompt v2 (launch-post style + dual-logo lockup rules: BISTEC Global left / Hearts Academy right in a white top bar) + three self-describing-filename LOGO artifacts (hearts transparent = kit.logoUrl, bistec colour, bistec reversed-white).
- Gates: tsc, lint (0 errors, 7 pre-existing warnings), **180/180 unit**, full mock **E2E 145 passed / 4 skipped / 0 failed** (pre-docs-feature baseline; targeted reruns green after), production build. **⚠️ Deploy:** `npx prisma migrate deploy` (**2 new migrations** `20260717085058`, `20260717114732`); no new env vars. **Machine-1 TODO:** run `node --env-file=.env scripts/fix-data-uri-logos.mjs --dry-run` then real (its Hearts kit has the same data-URI rows).

**✅ Cross-machine post transfer — IMPORT COMPLETED on machine 2 — 2026-07-17** (see `docs/handoff.md` top section):

- New script pair `scripts/export-posts.mjs` / `scripts/import-posts.mjs`: selective merge of library posts (Brief → Draft → revisions + terminal Post rows + all referenced MinIO objects) between the two dev machines. Idempotent import; owners remapped by username (`--owner` fallback), campaigns/kits re-linked by name or dropped to null. Verified end-to-end on machine 1 (real export 6 drafts / 31 objects / 60 MB; real import into the test DB; MinIO upload path smoke-tested).
- **Import ran on machine 2 the same day** (bundle landed in `Downloads\post-export`, not Documents): dry-run → real import → idempotency re-check, all matching the expected counts (6 briefs / 6 drafts / 16 revisions / 0 posts / 31 objects). Owner `adminbtg` resolved locally (no `--owner` needed); 1 campaign + 1 brand-kit link dropped by name-match as expected. DB spot-check: all 6 drafts EXPORTED with exportUrl, revision chains + `currentRevisionNumber` pointers intact (2 drafts are legacy zero-revision rows — null pointer is by design). No migrations, no new env vars.

**✅ Scheduler worker runtime-verified live + social publishing setup guide — 2026-07-16** (docs-only; see `docs/handoff.md` top section):

- **Scheduled generation runtime-verified end-to-end in CLI mode** (previously mock-only): real `ScheduledGeneration` → claimed → copy/background/design → Puppeteer render → `EXPORTED` draft in ~135s. Publish loop still not live-verified (no social credentials yet). Local worker run: `npx tsx --env-file=.env src/scheduler/worker.ts` (no package.json script). Worker self-recovers from DB outages (per-tick catch).
- **New [`docs/social-publishing-setup.md`](docs/social-publishing-setup.md)** — LinkedIn + Instagram account/token setup, credential wiring (`/admin/settings` → Social Channels beats env), full posting sequence + retry tables, troubleshooting. `docs/cold-start.md` §2 points to it.
- **Instagram image path via Cloudflare tunnel, validated:** presigned EXPORTS URLs are host-bound to `MINIO_ENDPOINT` (the signing client) — the tunnel URL must go there, NOT just `MINIO_PUBLIC_ENDPOINT`; keep `MINIO_PUBLIC_ENDPOINT=http://localhost:9000` so embedded asset URLs survive the ephemeral tunnel. Proven: external fetch of a tunnel-signed export → 200 `image/png`. LinkedIn needs no tunnel. **Ops (this machine):** `cloudflared` installed; `.env` reverted to localhost after testing; worker stopped.

**✅ Sidebar sign-out + brand-kit reference docs + campaign-voice images — 2026-07-14** (see `docs/handoff.md` top section):

- **Sign out button** at the bottom of the sidebar (`AppShell.tsx`) — the app's first sign-out; `authClient.signOut()` + hard redirect to `/login`.
- **Brand-kit reference documents:** kit Artifacts upload accepts PDF/DOCX/TXT/MD → new `REFERENCE_DOC` artifact type with `parsedText`/`truncated` (migration `20260714051500`), parsed once at upload (shared `parseDocumentText`). The brand-kit assistant grounds on feedToAI docs and now works docs-only (no image required); its ```brandkit block gained optional `colors` — **hex values stated explicitly in a document only** (validated), ranked ahead of the sampled palette. Never pixel-guessed.
- **Campaign voice accepts PNG/JPG:** stored unparsed in the private docs bucket and fed to the briefing chat via `runVisionModel` + presigned URLs (`collectCampaignDocImageUrls`). SVG still rejected.
- **MIME hardening:** the brandkit artifacts route (previously size-only for any file) now enforces raster-image / document allow-lists per artifact type.
- Gates: tsc, lint (0 errors, 7 pre-existing warnings), **161/161 unit**, full mock **E2E 135 passed / 4 skipped / 0 failed**, production build. **⚠️ Deploy:** `npx prisma migrate deploy` (1 new migration `20260714051500`); no new env vars.
- **Ops (this machine):** MinIO credentials rotated off `minioadmin` (the prod env gate rejects the default — symptom was 500s on every route incl. login). They live in `.env` AND `.env.test`; keep both in sync or storage E2E cases fail on MinIO auth. New vision surfaces are MOCK-verified only.

**✅ Brief draft autosave/recovery + modal-centering fix + expandable Recent Drafts — 2026-07-13** (specclaw change `brief-draft-recovery`; spec/design/tasks in `.specclaw/changes/brief-draft-recovery/`):

- **Brief draft recovery.** The brief wizard autosaves its working state (all fields, step position, uploaded-image refs) to a new **`BriefDraft`** table via a 1.5s-debounced `PUT /api/brief-drafts` once non-trivial (topic OR prompt OR an image); a half-written brief now survives tab close / refresh / device switch. Unfinished briefs surface as leading rows in the dashboard's **Recent Drafts** card ("Unfinished" chip + **Resume**/**Discard**); Resume opens `/brief?resume=<id>` and rehydrates everything (dangling template/kit ids are cleared by the wizard's existing consistency effect). Lifecycle rules live in **`src/lib/brief/briefDrafts.ts`** only: **5 rows/user** (oldest evicted), **7-day lazy TTL sweep** on read (F1's stale-draft precedent — no worker), image cleanup strictly scoped to `briefs/<userId>/` in the IMAGES bucket. Generate-success deletes the row **keeping** images (`DELETE …?keepImages=true` — the new `Brief.briefImages` references them); Discard/eviction/expiry delete images too. Routes are **owner-only with NO admin override** (private working state; foreign ids 404), and a PUT with an unknown id is 404 so a stale autosave can't resurrect a generated brief. Client-safe schema/helpers split into `briefDraftPayload.ts` (zod, 64 KB cap, traversal-guarded URL→key). Migration `20260713124851_brief_draft`.
- **Modal bleed fix (all centered modals — reported on the publish dialog).** `Modal` centers with `left/top 50% + -translate-x/y-1/2`, but `animate-scale-in`'s keyframe `transform` (with `fill: both`) **permanently replaced** the class transform, wiping the centering — the dialog's top-left corner sat at the viewport center and tall dialogs bled off-screen, unreachable by scroll. New `modalIn` keyframes (`globals.css`) carry `translate(-50%,-50%)` through every frame; `Modal.tsx` uses `animate-modal-in`. `ImageLightbox` (flex-centered) was never affected. **Never use `animate-scale-in` on a transform-centered element.**
- **Expandable Recent Drafts.** Dashboard card extracted to `src/components/dashboard/RecentDraftsCard.tsx` (client): collapsed = same 8 rows as before; **Expand** grows the card in place (max-h + internal scroll) showing the latest **25** from the single server query. Time labels are pre-formatted server-side (`relativeTime` uses `Date.now()` → would hydration-mismatch in a client component).
- Gates: tsc, lint (0 errors, 7 pre-existing warnings), **156/156 unit**, full mock **E2E 132 passed / 7 skipped** (new §P suite, 10 cases — incl. real-MinIO image-deletion assertion and admin-cannot-read-foreign-drafts), production build. **⚠️ Deploy:** `npx prisma migrate deploy` (1 new migration `20260713124851`; 4 total if coming from pre-F1–F6). No new env vars.
- Housekeeping: `.specclaw/config.yaml` model routing now says `claude` for all phases (the old `openai/gpt-5.1-codex` coding entry was unusable inside Claude Code and silently ignored), and the specclaw build gates are wired to the real `npm run test:unit` / `lint` / `build` commands.
- **Same-day live-testing outcomes:** CLI-mode **refine runtime-verified** (haiku background decision → sonnet refine → new revision, 76s). **⚠️ Stale-`.next` footgun:** `next dev` over a `.next/` from `npm run build` serves stale route modules (existing draft 404'd on `/revisions`/`/refine` while its GET 200'd) — `rm -rf .next` after any prod build (see `docs/cold-start.md` §6). Known refine caveat: the model can drop `__INLINE_ASSET_n__` placeholders (logged by `designAgentCli`), losing embedded images in that revision — version-switch back recovers; auto-reinsert is a candidate follow-up.

**✅ Six-feature batch: sizes + versioning + async gen + auto-scheduling + vision — 2026-07-13** (plans in `docs/plans/`):

Six features from a planning session, built in dependency order and merged to `main`. Full gates: tsc, lint (0 errors), **148/148 unit**, full mock **E2E 122 passed / 7 skipped**, production build.

- **F3 — Post sizes 1:1 / 4:5 / 9:16.** Added `STORY` (1080×1920) to the `AspectRatio` enum; **relabeled `PORTRAIT` as "4:5"** (its pixels are already 1080×1350 = 4:5 — pure label fix, no backfill). All sizes still resolve through `src/lib/aspectRatio.ts` (now also `nearestAspectRatio()` for F6). 9:16 publishes to the same feed channels. Migration `20260713120000`.
- **F2 — Free version switching.** New `Draft.currentRevisionNumber` pointer; generation records a **v1 "Original design"** revision; refine/regenerate append the new state and advance the pointer, so reverting moves back **and forward** to any version (fixes the old lost-forward bug). Restore reuses each revision's already-stored PNG instead of re-rendering (instant switch). Migration `20260713130000`.
- **F1 — Async generation + skeletons.** The wizard's `assemble-a/b` now **validate synchronously, return `202 {draftId}`, and generate in-process** (fire-and-forget via `src/lib/agent/backgroundGeneration.ts`); the draft page lands immediately with copy/image skeletons that resolve independently. Failure → `Draft.failureReason` + inline error + **Retry** (`POST /api/drafts/[id]/retry`); stale `IN_PROGRESS` drafts (>15 min) are swept to `FAILED` lazily on read. `generateDraft.ts` split into `createPendingDraft` + `runGenerationForDraft` (async) vs `generateDraftForBrief` (sync — MCP/ACP + scheduler unchanged, no orphan FAILED drafts). Migration `20260713140000`. **Deploy note: existing E2E generation callers now poll (`waitForDraft`).**
- **F4 — Chat-driven auto-scheduling.** The campaign briefing chat emits a ` ```schedule ` block; the admin reviews/edits/reorders the plan, then **batch-creates** queue entries (`POST /api/campaigns/[id]/queue/batch`, all-in-one-transaction, admin-only auto-publish gate). No migration.
- **F5 — Brand-kit from reference images (vision).** The app's **first real image-input path**: `src/lib/agent/vision.ts` (`runVisionModel` — Anthropic image blocks in API mode / `claude -p --allowedTools Read` on temp files in CLI mode). A brand-kit assistant chat grounds on the kit's `feedToAI` reference artifacts and proposes voice/tone/style/font-guesses (` ```brandkit ` block) + a **programmatically sampled** color palette (`sampleImageColors` in `puppeteer.ts` — font guesses are vision, colors are sampled, never vision-guessed). Apply = new voice prompt + kit colors. No migration.
- **F6 — Upload image → Path A template (vision).** "From image" turns an upload into a slot-based Path A template (sample content the fill agent replaces — **not** mustache tokens), opens it in the template editor, snaps the aspect ratio from the image (`getImageDimensions` + `nearestAspectRatio`, admin-overridable), and keeps the source as a `REFERENCE_IMAGE` artifact. `POST /api/admin/brandkits/[id]/templates/from-image`. No migration.

**⚠️ Deploy:** `npx prisma migrate deploy` applies **three** new migrations (`20260713120000`, `20260713130000`, `20260713140000`); no new env vars. **F5/F6 vision is MOCK-verified only** — the live API-image and CLI-`Read` paths aren't yet runtime-verified (the CLI mechanism was proven by a spike); smoke-test with real credentials before relying on it.

**✅ Per-user Claude OAuth tokens (CLI mode) — 2026-07-07** (see `docs/handoff.md` top section):

- Users connect their own Claude account at **`/settings`** (paste a `claude setup-token` token); in CLI mode their generations bill their own subscription. New `UserClaudeToken` model (migration `20260707164417`, AES-256-GCM + masked `keyPrefix`).
- **AsyncLocalStorage auth context**: routes wrap model-calling spans in `withClaudeAuth(userId, teamId, fn)` (`src/lib/agent/userToken.ts`); the single spawn site `runClaudeCli` reads it (`src/lib/agent/claudeAuth.ts`). ~~Callers that never set context — scheduler worker, MCP/ACP — always use the shared `CLAUDE_CODE_OAUTH_TOKEN`. Token precedence per spawn: user token → shared env token → dev's logged-in session.~~ **Superseded by team tenancy (re-verified 2026-07-27):** the wrapper is `withClaudeAuth` (not `withUserClaudeAuth`) and takes a **teamId**; precedence is exactly **personal token → team token**, and the shared-env-token and dev-session tiers were **deleted**. Scheduler and MCP call `withClaudeAuth(null, teamId, …)` — they resolve the **team** token, not an env one, and hard-fail without it. See the credential-chain bullet in the Outstanding-work section at the top.
- **Auth-failure handling:** typed `ClaudeCliError` + `isClaudeAuthFailure()`; on a rejected user token the call marks the row INVALID and retries ONCE on the shared credential. Save-time validation = live haiku ping (`MOCK_AI` seam for E2E; skipped/dormant in API mode).
- Routes: `GET/PUT/DELETE /api/me/claude-token` (withAuth, self-service); `GET /api/me` gained `cliMode` + `claudeToken`. UI: `/settings` nav item + `ClaudeTokenCard` + dismissible `ClaudeTokenPrompt` banner (CLI mode only).
- **Docker:** runner stage installs the Claude Code CLI (+ writable HOME) — VPS can run `DESIGN_PROVIDER=cli`; scheduled generation in the container now works in CLI mode (shared token). **MinIO pin bumped to `RELEASE.2025-09-07T16-13-09Z`** (old pin crash-loops on the volume's xl-meta-v3 format — do not pin back).
- Gates: tsc, lint, 135/135 unit, full E2E 109/0 (7 new §O cases), docker build. Deploy: `npx prisma migrate deploy` + image rebuild; no new env vars. Live-token flows not yet runtime-verified (needs a real `setup-token` token).

**✅ Post-brief Enhance with AI + full-screen export lightbox — 2026-07-07** (see `docs/handoff.md` top section):

- **Enhance with AI on the brief wizard Content step:** `enhancePostBrief()` (`src/lib/campaign/briefingAssistant.ts`) — per-post twin of the campaign-briefing enhance, same mode-agnostic Sonnet call, grounded in `resolveBrandKit(campaignId, brandKitId)` + campaign briefing/docs when a campaign is selected; drafts from just the topic. Route `POST /api/briefs/enhance` is **`withAuth` (editor-accessible)**, unlike the admin-only campaign enhance. UI: Before/After Accept/Discard flow in `ContentStep.tsx`; reuses the `buildMockBriefingEnhance` MOCK_AI seam; +1 §N E2E case.
- **Full-screen export preview:** shared `src/components/ui/ImageLightbox.tsx` (Radix Dialog, Frozen Light backdrop, topic + dimensions caption, blob-download button). Wired into the draft page Preview image (click-to-open) and library `PostCard` tiles (hover expand icon; tile click still navigates).
- No schema changes, no migrations, no new env vars.

**✅ Framework upgrade: Next.js 16.2 + React 19.2 — 2026-07-07** (see `docs/handoff.md` top section):

- **Async request APIs:** `withAuth` resolves the now-Promise route `params` centrally (`src/lib/api/handler.ts`) — wrapped handlers keep sync `{ params }`. `headers()` awaited in `auth.ts`. `src/middleware.ts` → **`src/proxy.ts`** (fn `proxy`).
- **next.config:** top-level `serverExternalPackages`; `experimental.proxyClientMaxBodySize: '16mb'` (Next 16 truncates >10MB bodies when a proxy exists — broke multipart uploads). Turbopack is the default builder.
- **Lint:** `next lint` removed → `eslint.config.mjs` flat config, `npm run lint` = `eslint .` (eslint 9). `react-hooks/set-state-in-effect` downgraded to warn (6 legacy hydration-init patterns).
- **Tooling majors:** lucide 1.x (brand icons → inline SVGs in settings), p-limit 7, vitest 4, lint-staged 17, @types/node 24; `test:e2e:serve` uses dotenv-cli. **One `next dev` per project** — stop :3000 before `test:e2e:serve`.
- **Deferred majors (backlog):** Prisma 7, Tailwind 4, zod 4, ESLint 10, TS 6, AI SDKs, puppeteer 25 — each a separate migration.
- Gates: tsc, lint, 98/98 unit, Turbopack build, full E2E green. Deploy: `npm install`; Node ≥ 20.9; no migrations.

**✅ Super-admin user management + username sign-in + AI briefing assistant — 2026-07-07** (see `docs/handoff.md` top section):

- **Role hierarchy:** `Role` enum gains `SUPER_ADMIN`; ALL role checks go through `hasRole()` (`src/lib/roles.ts`, pure) — never compare role strings. `withSuperAdmin` wrapper; `useCurrentUser().isSuperAdmin`. Seeded admin is SUPER_ADMIN; promote others via `scripts/promote-super-admin.mjs <email-or-username> [new-username]`.
- **Username sign-in** (better-auth `username()` plugin): login is by username (dev admin = **`adminBTG`**, password unchanged); email is internal only (synthetic `<username>@users.bistec.internal` for admin-created accounts). Gotcha: better-auth additionalField `role` default must be `"EDITOR"` (enum casing).
- **User management** at `/admin/users` (super-admin only): create (name/username/role/initial password), role toggle, **deactivate** (soft; sessions revoked + sign-in blocked via session-create hook) / reactivate, password reset. No self-modify, no touching super-admins.
- **AI briefing assistant** on the campaign page: upload PDF/DOCX/TXT/MD source docs (`CampaignDocument`, private `campaign-docs` bucket, max 5×10MB, parsed text capped), chat to converge on a briefing (` ```briefing ` block → "Apply to editor"), and an **Enhance with AI** before/after on the briefing editor. Sonnet via `src/lib/campaign/briefingAssistant.ts` (works in API + CLI modes; `MOCK_AI` seams). All admin-only; saving still uses the versioned briefing flow.
- **UI:** modals now scroll internally and never exceed the viewport (`Modal.tsx`); sidebar logo removed; navbar logo bigger.
- **⚠️ Deploy:** `npm install` (pdf-parse, mammoth) → `npx prisma migrate deploy` (`20260707065911`, `20260707135943`) → promote your admin. `pdf-parse`/`pdfjs-dist` are `serverComponentsExternalPackages` in `next.config.mjs` (webpack breaks pdfjs otherwise).
- Tests: 98/98 unit; E2E + 2 new suites green (`user-management`, `briefing-assistant`).

**✅ Campaign briefing + scheduled post generation — 2026-07-07** (see `docs/handoff.md` top section):

- **Versioned campaign briefing** (`CampaignBriefing`, mirrors `BrandKitPrompt`): campaign-level free-text context injected into every generation under the campaign (copy + Path A/B + background prompts; refine excluded) on top of the brand voice. `GET/POST /api/campaigns/[id]/briefing` + `[vid]/activate`; writes admin-only. Loader: `getActiveCampaignBriefing()` (`src/lib/campaign/briefing.ts`). `PROMPT_VERSION=2026-07-07.1`.
- **One generation orchestrator:** `generateDraftForBrief()` (`src/lib/agent/generateDraft.ts`) + new `runPathADesign` (`src/lib/agent/pathA.ts`); assemble-a/b + MCP are thin adapters.
- **Scheduled generation queue** (`ScheduledGeneration`): per-campaign planned posts with `generateAt` + postAction HOLD / SCHEDULE_PUBLISH / PUBLISH_NOW; routes under `/api/campaigns/[id]/queue` (list/create/edit/cancel/rerun). Editors plan HOLD; auto-publish actions admin-only. Worker (`generationRunner.ts`) mirrors H12 (SKIP LOCKED claim, 15-min lease, 3 retries 20/40/60-min backoff); post-actions create SCHEDULED Post rows handled by the existing publish scheduler. `worker.ts` runs two independent loops.
- **UI:** campaign page briefing editor + planned-posts queue table + entry modal; wizard shows the active briefing.
- **⚠️ Deploy:** `npx prisma migrate deploy` (migrations `20260707052036`, `20260707054311`). ~~Scheduled generation needs API mode in the Docker scheduler container (no `claude` CLI there)~~ **Superseded 2026-07-07:** the Docker image now ships the Claude Code CLI (per-user-token feature), so container scheduled generation works under `DESIGN_PROVIDER=cli` too (always on the shared `CLAUDE_CODE_OAUTH_TOKEN`).
- Tests: 72/72 unit; E2E baseline + 12 new cases in `tests/e2e/campaign-scheduling.test.ts` (needs `MOCK_AI` + test-DB access for the worker-flow cases).

**✅ Background images + CLI OAuth + Topic field + admin delete — 2026-07-03** (see `docs/handoff.md` top section):

- **AI background images (Path B + refine):** `src/lib/agent/background.ts` pre-step — Haiku decides `{needed, prompt}` (biased yes at generation, instruction-gated at refine) → gpt-image-2 via `resolveImageProvider()` → public IMAGES bucket → URL injected into the design/refine prompts and stored on `Draft.imageUrl`. Never fails the pipeline (skips to CSS/SVG on any error); `MOCK_AI` skips it. Portrait→`1024x1536`, square→`1024x1024` (`imageSizeFor`).
- **CLI-mode OAuth token:** set `CLAUDE_CODE_OAUTH_TOKEN` in `.env` (`claude setup-token`, ~1 yr) — headless `claude -p` spawns authenticate without the interactive login. API-key migration later = set `ANTHROPIC_API_KEY` + `DESIGN_PROVIDER=claude-html`, no code change.
- **Brief wizard Topic field:** short required Topic → `Brief.topic` (library card name); the big prompt textarea → `Brief.description`. No schema/API change.
- **Admin library delete:** `DELETE /api/drafts/[id]` (admin, transactional: posts → revisions → draft → orphaned brief) + trash button on `PostCard`.
- **⚠️ To activate:** fill `CLAUDE_CODE_OAUTH_TOKEN=` and `OPENAI_API_KEY=` in `.env`. ~~Background generation not yet runtime-verified with real keys~~ **Superseded 2026-07-16:** runtime-verified live — a scheduled generation produced a real gpt-image background (62.8s) into the IMAGES bucket end-to-end (unit + mock-E2E were already green: 55/55 unit, 80/0/4 E2E).

**✅ Improvement review fully remediated — 2026-07-02/03.** A four-reviewer whole-system design/code review found **77 findings** ([`docs/improvement-review-2026-07-02.md`](docs/improvement-review-2026-07-02.md)); **all 77 are remediated** across four phased commits on `main` (`689131cc`, `74725f28`, `b6fe63dd`, `8a1b2fae`). Structural changes to know about:

- **One design pipeline.** The `DesignOrchestrator` layer is deleted; web routes, CLI mode, and the MCP/ACP surface all run the same `runPathBDesign`/assemble-a core. Prompts are pure builders in `src/lib/agent/prompts/` (a `PROMPT_VERSION` is stamped on every Draft); model policy is `modelFor(path, mode)` in `src/lib/agent/config.ts` (Path A haiku / Path B sonnet, API + CLI variants).
- **Shared route infrastructure.** All session-authed API handlers use `withAuth`/`withAdmin` + zod `parseBody` (`src/lib/api/handler.ts`). Env config is centralized + validated in `src/lib/env.ts` (32 vars, fail-fast in production, skipped during `next build`).
- **One publish service.** `src/lib/publish/publishDraft.ts` owns the channel map + PENDING→PUBLISHED/FAILED machine; duplicate (draft, channel) publishes 409; ACP publishes record FAILED rows and respect draft status.
- **Frontend data layer.** React Query v5 (`QueryProvider`), typed `apiFetch<T = unknown>`, shared `src/lib/api-types.ts`, `useCurrentUser`; library uses `useInfiniteQuery`. Overlays are Radix (`src/components/ui/Modal.tsx`); errors via sonner toasts + `useConfirm()` (zero `alert()`/`confirm()`); admin is role-gated (nav + `admin/layout.tsx`). God components split: brief wizard → `src/components/brief/*` (117-line page), admin brandkits → `src/components/admin/brandkits/*`, draft page → `src/components/drafts/*`.
- **Deployment + gates.** The Docker prod image **builds** now (`output: 'standalone'`, `.dockerignore`, esbuild-bundled scheduler worker at `dist/scheduler/worker.js`); compose has healthchecks, loopback Postgres, pinned MinIO. CI runs lint + 45 vitest unit tests (`npm run test:unit`) + `npm run build` + `docker build` + the E2E suite. Renderer egress is allowlisted (MinIO + Google Fonts only — see `src/lib/renderer/puppeteer.ts`).
- **Migrations:** `20260702110000` (Brief.channels → `Channel[]` uppercase, `updatedAt` on Draft/Post, drop dead `CampaignDraft`) and `20260702113000` (`Draft.promptVersion`). Run `npx prisma migrate deploy` after pulling.

The earlier (2026-06) code review is also **fully remediated — all 28 fixes done**. Full details in **[`docs/code-review-findings.md`](docs/code-review-findings.md) → Remediation Status**.

The final 6 (the others landed earlier):

| ID  | Fix                                                                                                                                                                                | Migration                            |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ |
| H7  | Transaction atomicity — refine revision #, prompt version, posts create→publish wrapped in `$transaction` (P2002 → retry/409). Unique constraints already existed.                 | no                                   |
| H9  | Prisma indexes — `Post(status,scheduledAt)` + `(status,nextRetryAt)`, FK indexes, `BrandKit(isDefault,isDeleted)`.                                                                 | `20260623153740_h9_indexes`          |
| H12 | Scheduler atomic claim (`FOR UPDATE SKIP LOCKED`) + `PUBLISHING` lease + exponential-backoff retry (`retryCount`/`nextRetryAt`).                                                   | `20260623154752_h12_scheduler_claim` |
| H10 | Hybrid MinIO storage — public-read IMAGES/BRANDKITS buckets (stable URLs); private EXPORTS store object key, signed at read (`resolveExportUrl`). New `MINIO_PUBLIC_ENDPOINT` env. | no                                   |
| H11 | Puppeteer singleton browser + `p-limit` concurrency cap (`PUPPETEER_MAX_CONCURRENCY`, default 2).                                                                                  | no                                   |
| L2  | Shared `src/lib/apiFetch.ts` + `src/lib/brandkit/systemContext.ts`.                                                                                                                | no                                   |

> After pulling these, run `npx prisma migrate deploy` (or `migrate dev`) to apply the new migrations before starting the app. As of 2026-06-24 there is a third migration, `20260624120000_brief_brandkit` (adds `Brief.brandKitId`); as of 2026-06-30 a fourth, `20260630094723_aspect_ratio` (adds the `AspectRatio` enum + `Brief.aspectRatio` + `BrandKitTemplate.aspectRatio`).

**✅ Per-path CLI model split + generation diagnosis — 2026-07-01:** Both design paths were verified end-to-end (real Chromium render, no mocks) and the CLI model is now **per-path**, matching the API path.

- **Model split (both orchestrators): Path A (template fill) → Haiku, Path B (freeform) → Sonnet.** The API path already did this (`assemble-a` haiku, `pathB.ts`/`regenerate-design` sonnet, `refine` per `designMode`). The **CLI path** now matches: `runClaudeCli`/`runDesignAgentCli` take a per-call `model`, wired at `assemble-a` (`haiku`), `pathB.ts` (`sonnet`), and `refine` (`designMode==='TEMPLATE' ? haiku : sonnet`). **`CLAUDE_CLI_MODEL` is now a _global override_ — when unset, the per-path split applies; when set, it forces one model across every `claude -p` call** (copy included; copy defaults to haiku). `default` still omits `--model` (account default/Opus — avoid).
- **Diagnosis (CLI mode, keyless, current Claude account):** Path A → 54s, valid 2160×2160 PNG. Path B → 55s (Haiku) / 61s (Sonnet, richer 1017-char brief), both valid 2160×2160. No timeouts, no dropped assets, no credit runaway. The old Path B timeouts were the **Opus default**, not Sonnet — Sonnet finishes ~5× under the 300s budget. **Sonnet's Path B output is markedly richer** (feature-card grid, stat band, decorative motifs; 2.09 MB vs Haiku's 178 KB) — hence Sonnet for freeform, Haiku for template fill. Not runtime-verified after the split wiring (would re-burn credits); typecheck clean, logic mirrors the verified runs.

**✅ CLI timeout + credit-burn-on-timeout fix — 2026-06-30:** CLI-mode generation (`DESIGN_PROVIDER=cli`) could time out and produce no image **while still burning credits**. Fixed in `src/lib/agent/claudeCli.ts`:

- **Tree-kill on timeout (root cause of the burn).** On Windows the CLI runs via a `cmd.exe` shell (`spawn("claude.cmd", {shell:true})`), so the old `child.kill()` killed only the shell — `claude` kept running and billing after the timeout error. New `killTree()` uses `taskkill /pid <pid> /T /F` (win32) / SIGKILL (POSIX). Verified.
- **`--strict-mcp-config`** added to the spawn (zero MCP servers) so the headless run doesn't inherit the dev's session connectors (latency + context-bloat + cost).
- **Diagnostic logging** (`CLAUDE_CLI_DEBUG`, on by default, `0` to silence): spawn details, live stderr, a 20s heartbeat, and elapsed timing; calls tagged `"copy"`/`"design"`. Timeouts unchanged (design 300s, copy 120s); normal gen measured ~76s so it fits. See `docs/handoff.md` top section.

**✅ Post size picker + publish dialog + CLI model — 2026-06-30:** Three related changes landed:

- **The brief now picks a SIZE, not platforms.** Wizard step 1 ("Size & Design") offers **1:1 (1080×1080)** or **3:4 (1080×1350)**; channels default to both feeds and are chosen at _publish_ time instead. New `AspectRatio` enum on `Brief` + `BrandKitTemplate`. Pixel dims/labels live in the single source **`src/lib/aspectRatio.ts`** and are threaded through every render site (assemble-a, pathB, the design agent API + CLI, and the export/refine/restore routes + their prompts) so the canvas, the model instruction, and the preview never drift. The Path A template picker filters to the chosen size and `assemble-a` rejects a ratio mismatch (no stretching). Draft preview + library tiles reflect the ratio. Admin template create gets a size selector + badge; `scripts/seed-portrait-template.mjs` seeds a 3:4 template on the default kit.
- **Publish dialog on the draft page.** The library `PublishDialog` (channel checkboxes + optional schedule) is now a shared component (`src/components/library/PublishDialog.tsx`) wired into the draft review page's Publish button, replacing the old `confirm()`.
- **CLI mode model is configurable (`CLAUDE_CLI_MODEL`).** `claudeCli.ts` passes `--model` to `claude -p`; without a model the CLI used the costly account default (Opus), the root cause of CLI Path B burning credits. **As of 2026-07-01 the model is per-path by default (see the entry below) — `CLAUDE_CLI_MODEL` is now a global override, not the default source.**

**✅ Brand-kit selection + Hearts Talk fix — 2026-06-24:** Three related changes landed:

- **Oversized templates now work (Hearts Talk Path A fixed).** The orchestrator externalizes inline `data:` assets before the prompt and re-inlines them before render — see "Inline-asset externalization" below. The 600k CLI guard and the API's ~200k context are no longer hit; no re-seed needed.
- **Brand kit is selectable per brief** (independent of campaign) and the template picker filters to the selected kit (Path A + Path B). New `Brief.brandKitId`; precedence is now **explicit brief kit → campaign → project → system default**.
- **Brand kits are assignable on campaigns/projects** at create + edit (UI added; APIs already supported it). A campaign/project's assigned kit becomes the brief default; if none, the user picks one at the brief.

### Inline-asset externalization (the Hearts Talk fix)

`src/lib/agent/inlineAssets.ts` — `extractInlineAssets(html)` replaces every `data:` URI with a short `__INLINE_ASSET_n__` token (the model only ever sees the compact structural HTML — Hearts Talk shrinks 1.89 MB → 6.2 KB); `restoreInlineAssets(html, assets)` splices the originals back just before Puppeteer renders (byte-for-byte lossless). Threaded through `DesignAgentOptions.inlineAssets`, the CLI runner (`designAgentCli.ts`, with a template-fill instruction that tells Claude to preserve the placeholders), the API tool-use runner (`designAgent.ts`), and `assemble-a/route.ts`. Generic to any oversized template — not specific to Hearts Talk.

> Before testing/running, follow `docs/cold-start.md` §0 preflight. Dev server runs on `http://localhost:3000`. ~~CLI-mode generation (`DESIGN_PROVIDER=cli`) needs the seeded `cli` provider (`scripts/seed-cli-provider.mjs`).~~ **No longer true (PR #30 + PR #40):** CLI mode needs **no COPY provider**. Note the local Bistec team still has that seeded row — it is why the PR #40 wizard bug never reproduced locally, so when testing provider-gating behaviour use a team **without** it.

## What this project is

A Next.js 14 + TypeScript web app that turns a brief into a finished, on-brand, ready-to-publish social media post (Instagram + LinkedIn). Two generation paths:

- **Path A** — HTML/CSS brand template (stored in DB), Claude agent fills it → Puppeteer renders PNG
- **Path B** — Claude agent generates freeform HTML/CSS design → Puppeteer renders PNG

Stack: Next.js 14, TypeScript, Prisma, PostgreSQL, MinIO, better-auth (self-hosted), Docker Compose. Puppeteer (headless Chromium) for HTML→PNG rendering. Claude agent harness (Anthropic SDK tool-use loop) drives design generation.

## Key reference documents

### UI / Design system

Before building or modifying any page, read the design system:

- **[`docs/ui-reference/DESIGN_SYSTEM.md`](docs/ui-reference/DESIGN_SYSTEM.md)** — the design system for bistec-studio. Read this before writing any component or page. Also reference [`docs/ui-reference/screen-dark.png`](docs/ui-reference/screen-dark.png) and [`docs/ui-reference/screen-light.png`](docs/ui-reference/screen-light.png) for visual reference, and [`docs/ui-reference/synthetix-original-reference.html`](docs/ui-reference/synthetix-original-reference.html) for the source HTML reference.

### Architecture & technical design

Before writing any backend code, API routes, Prisma models, or provider logic, read the design document:

- **[`.specclaw/changes/marketing-post-studio-v1/design.md`](.specclaw/changes/marketing-post-studio-v1/design.md)** — authoritative source for the Prisma schema, all API route contracts, the AI provider abstraction layer (`CopyProvider`, `ImageProvider`, `DesignOrchestrator` interfaces), the Claude design agent harness, the Puppeteer renderer, MinIO integration, AGUI backend flow, provider registration encryption, and the full file/folder structure of the real app. Any implementation that touches data models, API routes, or provider logic must align with this document.
- **[`docs/mcp-acp-guide.md`](docs/mcp-acp-guide.md)** — how the MCP (stdio) and ACP (HTTP) machine-access surfaces work: `bstk_` team API keys (mint/resolve/revoke), the MCP tool set + client config (incl. **where `MCP_API_KEY` goes**), the ACP capabilities + curl examples, team-scoping guardrails, and the CLI prompt-injection / SSRF hardening. Read before touching `src/mcp/*`, `src/app/api/acp/*`, or the API-key routes.

### Code review & remediation status

- **[`docs/code-review-findings.md`](docs/code-review-findings.md)** — full code review (42 findings) + **Remediation Status** (✅ all 28 fixed as of 2026-06-23) plus the one open known issue (oversized "Hearts Talk" template breaks Path A). **Read this before picking up review/remediation work.**

### Testing

- **[`docs/e2e-test-plan.md`](docs/e2e-test-plan.md)** — the authoritative E2E test design + catalog. **The full catalog (§A–§Q) is implemented & green** (145 passed / 0 failed / 4 skipped confirmed 2026-07-17 — the 4 intentional skips below; the 3 §J ACP-auth cases need `BISTEC_API_KEYS` in `.env.test` (CI sets them; skipped without them); §P brief-draft-recovery, §Q async-draft-actions added). Mock seams live in `src/lib/testHooks.ts` (gated by `MOCK_AI`/`MOCK_PUPPETEER`/`MOCK_SOCIAL`, dormant in prod); run against a dedicated `bistec_studio_test` DB. Reproduce: `npm run test:e2e:db` → `npm run test:e2e:serve` → `npm run test:e2e:mock` (plan §0). **CI gate:** `.github/workflows/e2e.yml` runs the whole suite — including the §K security-fix regressions — on every PR + push to `main`. **⚠️ `.env.test` must use `DESIGN_PROVIDER=claude-html` (not `cli`) — CLI mode bypasses the `MOCK_AI` seam and times out generation tests.**
  - Test-only seams added for the catalog (all dormant in prod): `buildMockCopy`/`shouldMockPublishFail` in `testHooks.ts` (deterministic publish failures via a `__FAIL_*__` sentinel in the brief topic), and `POST /api/test/scheduler-tick` (double-gated: 404 in `NODE_ENV==='production'`, 404 unless `MOCK_SOCIAL`, admin-only) so §K H12 can drive the scheduler over HTTP. **CI runs the app in `next dev` mode** because that seam is intentionally inert in a prod build.
  - The 4 intentional skips: TC-GEN-05 (needs a mock IMAGE-provider seam), TC-REG-H11a/b/c (real-Chromium / host-process observation).

### Specification & planning

- **[`docs/handoff.md`](docs/handoff.md)** — session handoff with current decisions, Path A/B design descriptions, AGUI spec, provider registration flow, v2 interoperability target, and the latest code-review remediation summary
- **[`.specclaw/changes/marketing-post-studio-v1/spec.md`](.specclaw/changes/marketing-post-studio-v1/spec.md)** — full functional requirements (FR-01 through FR-33) and non-functional requirements
- **[`.specclaw/changes/marketing-post-studio-v1/design.md`](.specclaw/changes/marketing-post-studio-v1/design.md)** — architecture, Prisma schema, API routes, provider abstraction layer, file tree
- **[`.specclaw/changes/marketing-post-studio-v1/tasks.md`](.specclaw/changes/marketing-post-studio-v1/tasks.md)** — 28 tasks across 6 waves with estimates and dependencies
- **[`.specclaw/changes/marketing-post-studio-v1/proposal.md`](.specclaw/changes/marketing-post-studio-v1/proposal.md)** — original proposal + post-proposal decisions log

### Per-wave execution plans

| Wave | File                                                                                                  | Scope                                                  |
| ---- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| 1    | [wave-1-scaffold.md](.specclaw/changes/marketing-post-studio-v1/wave-1-scaffold.md)                   | App scaffold, Docker Compose, Prisma, Clerk            |
| 2    | [wave-2-providers.md](.specclaw/changes/marketing-post-studio-v1/wave-2-providers.md)                 | AI provider abstraction layer                          |
| 3    | [wave-3-canva-minio.md](.specclaw/changes/marketing-post-studio-v1/wave-3-canva-minio.md)             | HTML renderer (Puppeteer) + Claude design agent, MinIO |
| 3b   | [wave-3b-brand-data-layer.md](.specclaw/changes/marketing-post-studio-v1/wave-3b-brand-data-layer.md) | Brand kit data layer                                   |
| 4    | [wave-4-generation.md](.specclaw/changes/marketing-post-studio-v1/wave-4-generation.md)               | Brief → generation pipeline (Path A + B)               |
| 5    | [wave-5-publishing.md](.specclaw/changes/marketing-post-studio-v1/wave-5-publishing.md)               | Publishing + scheduler                                 |
| 6    | [wave-6-admin-e2e.md](.specclaw/changes/marketing-post-studio-v1/wave-6-admin-e2e.md)                 | Admin settings, AGUI refinement, E2E tests             |

## Architecture decisions to remember

- All AI calls are **server-side only** — the browser never calls an AI API or Puppeteer directly
- **Roles are hierarchical:** `SUPER_ADMIN > ADMIN > EDITOR`; every check goes through `hasRole()` (`src/lib/roles.ts`) — never compare role strings directly. Super-admins pass every admin gate. User management (`/admin/users`) is super-admin-only; "delete" = deactivate (`User.disabled`, sessions revoked, sign-in blocked).
- **Sign-in is by username** (better-auth `username()` plugin); email exists internally only (`<username>@users.bistec.internal` synthesized for admin-created accounts). Seeded dev admin: `adminBTG`.
- **Brand kit precedence:** Explicit brief kit (`Brief.brandKitId`) → Campaign kit → Project default → system default (`BrandKit.isDefault = true`)
- **Post size:** chosen per brief (`Brief.aspectRatio`: `SQUARE`=1080×1080, `PORTRAIT`=1080×1350); a brand template declares the size it was designed for (`BrandKitTemplate.aspectRatio`). Dimensions resolve through `src/lib/aspectRatio.ts` — the only place pixel sizes are defined.
- **AI provider resolution order:** Brief's chosen key → `AvailableProvider.isDefault` → env var fallback
- **API keys** stored AES-256-GCM encrypted; only `keyPrefix` shown in UI after registration; full key never returned
- **MinIO** served to browser via pre-signed URLs only — MinIO port never publicly exposed
- **Claude design agent** runs as a tool-use agent loop — tools: `generateImage`, `renderHtml`, `getBrandKitContext`
- **AGUI refinement:** natural language → Claude updates HTML → Puppeteer re-renders → `DraftRevision(htmlSnapshot)`
- **Brand kit structured data** (colors, fonts, logoUrl) is stored in the DB and passed to Claude as CSS variable definitions
